server.port=8090
#spring.datasource.url=jdbc:hsqldb:hsql://localhost:9001

spring.datasource.username=SA 
spring.datasource.password=
#spring.datasource.driver-class-name=org.hsqldb.jdbcDriver
spring.jpa.hibernate.ddl-auto=create
#Indica que debe crear el modelo de datos con los campos indicados ->Si mantenemos este atributo cada vez ç
# que ejecutamos la aplicación se van a crear las tablas (es decir vamos a perder todos los datos).

# No crear ninguna tabla, solo validar:
# spring.jpa.hibernate.ddl-auto=validate
# Crear la tabla de nuevo #spring.jpa.hibernate.ddl-auto=create